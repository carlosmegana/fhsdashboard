// Placeholder body for pages whose content is still being defined.
export default function ComingSoon({ children }: { children: React.ReactNode }) {
  return (
    <div className="rounded-lg border border-dashed border-line-2 px-6 py-16 text-center">
      <p className="text-sm text-ink-3">{children}</p>
    </div>
  );
}
